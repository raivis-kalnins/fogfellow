<?php
/**
 * Block Name: Call To Action
 *
 * @package dp
 */

dp_get_template_part(
	'resources/partials/cta/front-page',
	'',
	array(
		'',
	)
);


// This will need to be setup once the courses theme is done;
// if ( 'course' === $post_object->post_type || 'course_mode' === $post_object->post_type ) :

// Get the first course mode.
// Get the last course mode.

// $pre_title = get_field( 'post_title' ) ? get_field( 'post_title' ) : date( $post_object->post_date );
// endif;
